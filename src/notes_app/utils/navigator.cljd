(ns notes-app.utils.navigator)

(defn navigate-to-screen [navigator route-name]
  (.pushNamed navigator route-name))

(defn navigate-to-back [navigator]
  (.pop navigator))

(defn navigate-to-home [navigator]
  (.popUntil navigator 
             (fn [route] (true? (.-isFirst route)))))